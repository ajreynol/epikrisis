# The commit of anoieu whose policy checker this repository is checked by.
#
# A pin is a choice, not a fact: moving it changes what decides whether this
# tree complies. The ecosystem's rule is that a pin only moves to a commit
# where the other project's CI is green, which is what `eo_bump` enforces and
# is the reason this file is not edited by hand.
#
# The checker is `scripts/policy_check.py` and the contract asked for is `1`.
# Both are named in `.github/workflows/anoieu.yml`. It moved out of `tools/`
# in anoieu after the commit this repository pinned before this file existed,
# which is why that workflow still looks in the old place and says so.
154228a40d21584b95f4029742ccc8f432ea87f5
